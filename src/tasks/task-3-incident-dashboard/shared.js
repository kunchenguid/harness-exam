export const VALID_TABS = new Set([
  "overview",
  "deployments",
  "feature-flags",
  "trace-samples",
  "customer-impact",
]);

export const VALID_REGIONS = new Set(["all", "eu", "us", "enterprise"]);

export const VALID_SORTS = new Set(["arr", "first-failure"]);
